[project]
name = "memory-palace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipython>=8.37.0",
    "pika>=1.3.2",
    "python-dotenv>=1.1.1",
    "requests>=2.32.4",
]
